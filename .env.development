# Dev pointé sur le backend Railway (pas besoin de lancer un backend local).
VITE_API_URL=http://localhost:3000
# VITE_API_URL=https://elocation-backend-production.up.railway.app

# Vide = temps réel désactivé (Railway n'expose pas encore de gateway WebSocket).
# Pour tester le WS en local : VITE_WS_URL=ws://localhost:3001
VITE_WS_URL=

VITE_VAPID_PUBLIC_KEY=
