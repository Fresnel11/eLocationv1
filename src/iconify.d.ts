/// <reference types="unplugin-icons/types/react" />

/**
 * Types des imports « ~icons/<jeu>/<nom> ».
 *
 * Jeu de référence du projet : line-md (Material Line Icons), animé nativement
 * en SVG. On ne pioche dans un autre jeu Iconify que si l'icône n'existe pas
 * dans line-md.
 */
